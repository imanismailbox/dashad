import defaultColors from 'tailwindcss/colors';

const colors = {
  ...defaultColors,
  primary: defaultColors.indigo,
  secondary: defaultColors.gray,
  dark: defaultColors.slate,
};

export default colors;
